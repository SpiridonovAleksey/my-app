import React from "react";
import "./users.css"
import UsersList from "./users-list/users-list";

const Users = (props) => {
  return (
    <section>
      <UsersList/>
    </section>
  )
};

export default Users;
